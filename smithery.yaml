runtime: "container"

startCommand:
  type: "http"
  configSchema:
    type: "object"
    required: ["apiKey"]
    properties:
      apiKey:
        type: "string"
        title: "API Key"
        description: "Your API key"

build:
  dockerfile: "Dockerfile"
  dockerBuildPath: "."
